[package]
name = "wraithphone"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Deterministic claim metadata and staged proof integration plans."
repository = "https://github.com/Kinhold/WRAITHPHONE"

[dependencies]
blake3 = "=1.8.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
